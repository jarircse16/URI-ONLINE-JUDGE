#include<bits\stdc++.h>
using namespace std;
int main()
{
    int T,i;
    while(T--)
    {
        int N,M;
        cin>>N>>M;
        int a[N];
        for(i=0;i<N;i++)
        {
            cin>>a[i];
        }
        sort(a,a+n);
        __gcd()
    }
}
